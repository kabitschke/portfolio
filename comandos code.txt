shift+alt+f = formatar documento
alt = seleciona varias linhas para edi��o
! preenche o html
div.container = cria a div e ja inclui a class
Selecionar um texto e apertar ALT para mover
div.classe>div = cria div com a classe e uma div dentro da principal
div.hamburguer>div.line*3  = cria div com a classe e uma div dentro da principal e mais tres divs com a class line
div.hamburguer>div.line#line*3
div.sidebar>nav>ul.menu>(li.menu-item>a.menu-link)*5








https://unsplash.com/photos/DuHKoV44prg
unsplash
uigradiente
https://www.webfx.com/tools/emoji-cheat-sheet/     EMOJIS

git config --global user.name "Maycon Kabitschke"

git config --global user.email "mayconkabitschke@gmail.com"

git config --global core.editor vscode

git config user.name  pegar o nome

git config --list lista todas as informações 

cd ../ volta diretorio

cd c:/

code . abre o vscode

cd c:/xampp/htdocs   pasta onde estão os arquivos

mkdir modulogit cria  a pasta 

git init cria o repositorio

git log  /Puxa todas os commits

git commit -m "Primeiro commit"

git commit -am "comentario"   /adiciona os arquivos e faz o commit

git add -A  /adiciona todos os arquivos criados recentemente

-------------------------------------------------------

git add . ADICIONA TODOS OS ARQUIVOS

git commit -m "Primeiro commit"

git push ENVIA OS ARQUIVOS PARA O GITHUB

git pull origin Recebe os arquivos

git clone urldoprojeto

git remote -v Para onde o repositorio está apontando


----------------------------------------------------------

git branch /saber qual o nivel do usuario

git status / ver as alterações que foram feitas

git reset --hard 05500cb08eead54fa568519c351c4a3f15cd8a11 /volta para o commit escolhido e exclui os outros

git reset --soft 05500cb08eead54fa568519c351c4a3f15cd8a11 / volta para o commit escolhido e mantem os outros

git branch teste / cria um novo branch com nome teste

git checkout teste  / troca de branch entra no branch teste

git diff /mostra todas as alterações feitas

git diff --name-only  //lista os arquivos que foram alterados 

git diff style.css //lista apenas modificações no style.css

git checkout HEAD  --nomedoarquivo // não fará a alteração no arquivo selecionado/volta ao estado original do arquivo 

-------------------------------------------------------------------------

git bash 

$ ssh-keygen -t rsa -b 4096 -C "mayconkabitschke@gmail.com"

git init //para iniciar seu diretório

git remote add origin https://github.com/kabitschke/modulogit-.git

link https://www.youtube.com/watch?v=O2DFKHla80A subir arquivos para o git

git push origin main //envia os arquivos para o github


arquivo .gitignore //ignora arquivos selecionados *.sql *.txt ignora todas as extenções sql e txt / ignorar pasta 
nomedapasta/*

git revert --no-edit 81a40f61a14bb52399b754eb90bc2b018385b8ac //volta para o commit anterior, so inserir o codigo
do commit

git push origin :teste // Deleta o branch teste la no gitHub
git branch -D teste // Deleta o branch teste local 


git pull origin main // Pega os arquivos do repositório online gitHub

git clone urldoprojeto